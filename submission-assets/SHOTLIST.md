# Screenshots for SUBMISSION.html

Drop the PNGs into this folder with these exact names. The page picks them up on reload; a missing image simply hides its slot, so the doc stays clean until each one lands.

| File name | Where to take it | What to capture |
|---|---|---|
| `dashboard-conversation.png` | localhost:3000/dashboard, Conversations tab, Price Haggler | The chat with a said versus heard pair visible, audio player at the top. This is the full width hero shot |
| `dashboard-report.png` | localhost:3000/dashboard, Report tab | The scorecard with dimensions open and the defect list |
| `persona-studio.png` | localhost:5174 | The studio with the persona report visible |

Tips: use a clean window (hide bookmarks bar), light theme is fine (the dashboard is themed), and capture at full width so text is legible. Retina resolution preferred.
